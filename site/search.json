{"config":{"separator":"[\\s\\-_,:!=\\[\\]()\\\\\"`/]+|\\.(?!\\d)"},"items":[{"location":"","level":1,"title":"KernelBox","text":"","path":["KernelBox"],"tags":[]},{"location":"#kernelbox","level":1,"title":"KernelBox","text":"<p>Persistent IPython kernels for agents, scripts, and tools — no notebook server required.</p> <p>KernelBox talks directly to IPython kernels over ZeroMQ through <code>jupyter_client</code>. It gives your agent a persistent Python session it can hold open, run code in, and read structured results from — without spinning up a full Jupyter server.</p>","path":["KernelBox"],"tags":[]},{"location":"#the-30-second-pitch","level":2,"title":"The 30-second pitch","text":"<pre><code>from kernelbox import get_or_create, execute, destroy\n\nkernel = get_or_create(\"demo\")\n\n# Step 1 — load data\nexecute(kernel, \"numbers = [1, 2, 3, 4, 5]\")\n\n# Step 2 — the kernel still remembers `numbers`\nresult = execute(kernel, \"sum(numbers)\")\nprint(result.return_value)  # 15\n\ndestroy(\"demo\")\n</code></pre> <p>State persists between calls. No re-running. No re-serialising into the LLM prompt.</p>","path":["KernelBox"],"tags":[]},{"location":"#pick-your-interface","level":2,"title":"Pick your interface","text":"Interface Best for Python API Writing agents or automation scripts FastAPI service HTTP endpoints, Swagger UI, external tools CLI Shell scripts, quick one-off commands","path":["KernelBox"],"tags":[]},{"location":"#what-it-does","level":2,"title":"What it does","text":"<ul> <li> <p>Creates and manages named kernel sessions — <code>get_or_create(\"name\")</code> returns a live kernel, creating it if needed.</p> </li> <li> <p>Executes Python or Bash code — <code>execute(kernel, code)</code> runs the code and returns a rich structured result.</p> </li> <li> <p>Retries failing code automatically — <code>execute_with_retry</code> calls your repair function after each failure so your agent can self-correct.</p> </li> <li> <p>Exposes a full REST API — the FastAPI server gives you <code>POST /sessions/{name}/execute</code>, <code>GET /kernels</code>, and more.</p> </li> <li> <p>Works from the terminal — the <code>kernelbox</code> CLI manages kernels and executes code with JSON output, easy to pipe into <code>jq</code>.</p> </li> <li> <p>Persists kernel registry to disk — the file backend survives server restarts; swap to memory backend for ephemeral use.</p> </li> <li> <p>Runs securely in Docker — the included <code>docker-compose.yml</code> uses a non-root user, read-only filesystem, and tmpfs scratch space.</p> </li> </ul>","path":["KernelBox"],"tags":[]},{"location":"#assumptions-current-limitations","level":2,"title":"Assumptions &amp; Current Limitations","text":"<p>Know these before going to production</p> <p>These are known constraints in v0.1.0. Understanding them will help you deploy safely.</p> Area Behaviour in v0.1.0 Sandboxing Kernels run with the same OS privileges as the calling process. Use the included Docker setup for isolation. Authentication The FastAPI server has no auth. Bind to <code>127.0.0.1</code> or place behind a reverse proxy. Concurrency File registry is not safe for heavy concurrent writes from multiple processes. Idle kernel cleanup KernelBox tracks idle timeout in metadata but does not automatically kill idle kernels. Output truncation stdout/stderr is cut at <code>KERNELBOX_OUTPUT_CHAR_LIMIT</code> (default 10 000 chars). Check <code>result.truncated</code>. Bash execution Bash runs through IPython's <code>%%bash</code> magic — not a real shell. Path and env may differ. Windows ACL The Windows ACL restriction for Jupyter connection files is patched. Connection files may be world-readable. Kernel types Only the <code>python3</code> kernelspec is tested. Other kernelspecs may work but are unsupported.","path":["KernelBox"],"tags":[]},{"location":"#where-to-go-next","level":2,"title":"Where to go next","text":"Setup Install and run for the first time Python API Full walkthrough with all return fields HTTP API curl examples and Swagger UI CLI Terminal commands and JSON output Configuration All environment variables Security Read before production","path":["KernelBox"],"tags":[]},{"location":"about/","level":1,"title":"About KernelBox","text":"<p>Have you ever watched an AI agent try to execute Python code, fail spectacularly because of a missing dependency or a state mismatch, and then spiral into an endless hallucination loop? Yeah, me too.</p>","path":["About KernelBox"],"tags":[]},{"location":"about/#the-story-why-i-created-kernelbox","level":2,"title":"The Story: Why I Created KernelBox","text":"<p>I was building autonomous coding agents and kept hitting a brick wall. The LLMs were getting smarter, but their \"hands\" were tied. Running code in ephemeral sandboxes or relying on heavy notebook servers was slow. Worst of all, state was constantly lost between steps. Getting error feedback that was reliable enough for an agent to actually fix its own problem was a complete nightmare.</p> <p>I needed a way for my agents to hold a REPL open, install packages on the fly, and keep their variables in memory—just like a human developer does.</p>","path":["About KernelBox"],"tags":[]},{"location":"about/#the-current-challenges-in-autonomous-agentic-code-execution","level":2,"title":"The Current Challenges in Autonomous Agentic Code Execution","text":"<p>If you've built AI agents, you know the struggle:</p> <ol> <li> <p>The Subprocess Trap</p> <p>Running raw Python scripts in subprocesses means the agent loses state instantly. If it needs to run 10 lines of <code>pandas</code>, check the dataframe shape, and then run 10 more lines based on that, it has to re-run the entire script from scratch.</p> </li> <li> <p>The Token-Hungry Memory Layer</p> <p>If you try to fix the subprocess trap by building a \"memory layer\" (feeding previous code outputs, massive stringified dataframes, and variable states back into the LLM prompt for every single turn), your token usage explodes. It's slow, expensive, and easily exhausts the context window.</p> </li> <li> <p>The Bloated Server</p> <p>The standard solution is spinning up full Jupyter servers. This is heavy, requires managing REST APIs and websockets, and is overkill for an agent that just wants to talk to a kernel.</p> </li> <li> <p>Fragile Error Handling</p> <p>When a script fails, the agent often gets a massive, unreadable stack trace.</p> </li> </ol>","path":["About KernelBox"],"tags":[]},{"location":"about/#what-i-solved","level":2,"title":"What I Solved","text":"<p>I built KernelBox to solve this exact bottleneck. It talks directly to IPython kernels through <code>jupyter_client</code>.</p> <ul> <li>No bloated web server.</li> <li>No daemon process.</li> <li>Just a clean, direct line from the agent's brain to the execution environment.</li> </ul> <p>Massive Token Savings: KernelBox holds the session and memory natively inside the kernel. Your agent executes code block A, and the kernel remembers the data frame. When the agent executes block B, the data is still right there. You don't need to dump the entire data structure back into the LLM's context window! This saves a staggering amount of tokens, speeds up inference, and prevents context limit errors. You just pass the tiny, specific output the agent needs right now, and the heavy data stays safely in the kernel's RAM.</p> <p>The state persists, the output is rich and structured, and error traces are clean. If the agent makes a mistake, our built-in <code>Retry API</code> handles it gracefully, allowing the agent to self-correct in real-time.</p>","path":["About KernelBox"],"tags":[]},{"location":"about/#fast-secure-and-sandboxing-trade-offs","level":2,"title":"Fast, Secure, and Sandboxing Trade-offs","text":"<p>KernelBox is lightning fast because it cuts out the middleman. There is zero HTTP network overhead for local kernel communication; it communicates over ZeroMQ directly with the execution kernel.</p> <p>Let's talk about Security vs. Dedicated Sandboxes. Many agent frameworks spin up heavy, fully isolated Docker containers (dedicated sandboxes) for every single run. That is incredibly secure, but it's also painfully slow and resource-heavy.</p> <p>KernelBox takes a different approach: it is an un-opinionated, lightning-fast pipe to an IPython kernel. It runs exactly where you deploy it.</p> <p>The Limitation / Trade-off: If you run KernelBox natively on your personal laptop, the agent has access to your laptop's file system. If you want strict security and isolation, you should run KernelBox inside a Docker container or a remote VM. KernelBox doesn't build the walls; it gives you the best engine to run code inside whatever walls you choose to build.</p>","path":["About KernelBox"],"tags":[]},{"location":"about/#what-is-not-there","level":2,"title":"What is NOT There","text":"<p>I believe in keeping things lightweight and focused:</p> <ul> <li>No built-in Docker management: KernelBox doesn't spin up Docker containers for you out of the box (though you can easily run it inside one).</li> <li>No heavy cloud infrastructure: It's not a hosted cloud sandbox solution. It's a local utility package.</li> <li>Unopinionated: It doesn't care how your agents think, it just gives them a flawless place to execute code.</li> </ul>","path":["About KernelBox"],"tags":[]},{"location":"about/#the-ultimate-usecase","level":2,"title":"The Ultimate Usecase","text":"<p>Imagine an autonomous data scientist agent. You give it a massive dataset. It loads the dataset into memory, inspects the columns, and realizes it needs a missing library. It installs it on the fly, runs some exploratory analysis, generates a plot, and returns it to you—all in the same continuous, stateful session.</p> <p>That is the power of KernelBox. Welcome to frictionless agentic code execution!</p>","path":["About KernelBox"],"tags":[]},{"location":"configuration/","level":1,"title":"Configuration","text":"<p>KernelBox is configured entirely through environment variables. No config file is needed.</p> <p>Variables are read once at import time. Restart the process after changing them.</p>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#all-variables","level":2,"title":"All variables","text":"Variable Default Description <code>KERNELBOX_MAX_RETRIES</code> <code>5</code> Maximum attempts in <code>execute_with_retry</code> before returning <code>status=\"max_attempts_exceeded\"</code> <code>KERNELBOX_EXECUTION_TIMEOUT</code> <code>60</code> Seconds before a single code execution is cancelled and returns <code>status=\"timeout\"</code> <code>KERNELBOX_KERNEL_IDLE_TIMEOUT</code> <code>1800</code> Seconds of inactivity before a kernel is considered stale (metadata only — kernels are not killed automatically) <code>KERNELBOX_OUTPUT_CHAR_LIMIT</code> <code>10000</code> Maximum characters of stdout + stderr captured per execution. Output beyond this is dropped and <code>result.truncated</code> is set to <code>True</code> <code>KERNELBOX_STORE_BACKEND</code> <code>file</code> <code>file</code> — persists registry to disk across restarts. <code>memory</code> — in-process only, lost when the process exits <code>KERNELBOX_REGISTRY_PATH</code> <code>.kernelbox/registry.json</code> (Windows) / <code>$TMPDIR/kernelbox/registry.json</code> Path to the JSON file used by the file registry backend <code>KERNELBOX_RUNTIME_DIR</code> <code>.kernelbox/runtime</code> (Windows) / <code>$TMPDIR/kernelbox/runtime</code> Directory where Jupyter kernel connection files are stored <code>KERNELBOX_STARTUP_CODE</code> (unset) Python code executed automatically after every new kernel is created. Useful for pre-importing libraries or setting up the environment <code>KERNELBOX_KERNEL_TYPE</code> <code>python3</code> The Jupyter kernelspec name to use when starting kernels. Run <code>jupyter kernelspec list</code> to see what's installed <code>KERNELBOX_HOME</code> (unset) If set, overrides the base directory for both <code>KERNELBOX_REGISTRY_PATH</code> and <code>KERNELBOX_RUNTIME_DIR</code>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#setting-variables","level":2,"title":"Setting variables","text":"PowerShellBash / zsh.env file (manual) <pre><code>$env:KERNELBOX_EXECUTION_TIMEOUT = \"120\"\n$env:KERNELBOX_STARTUP_CODE = \"import numpy as np; import pandas as pd\"\nuv run fastapi dev --port 8080\n</code></pre> <pre><code>export KERNELBOX_EXECUTION_TIMEOUT=120\nexport KERNELBOX_STARTUP_CODE=\"import numpy as np; import pandas as pd\"\nuv run fastapi dev --port 8080\n</code></pre> <p>KernelBox does not load <code>.env</code> files automatically. You can source one manually:</p> <pre><code># .env\nKERNELBOX_EXECUTION_TIMEOUT=120\nKERNELBOX_STORE_BACKEND=memory\n</code></pre> <pre><code>set -a &amp;&amp; source .env &amp;&amp; set +a\nuv run fastapi dev --port 8080\n</code></pre>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#common-recipes","level":2,"title":"Common recipes","text":"","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#pre-import-libraries-on-every-kernel-start","level":3,"title":"Pre-import libraries on every kernel start","text":"<pre><code>export KERNELBOX_STARTUP_CODE=\"import numpy as np; import pandas as pd; import matplotlib.pyplot as plt\"\n</code></pre> <p>Every new kernel will have <code>np</code>, <code>pd</code>, and <code>plt</code> ready to use without an explicit import step.</p>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#use-a-faster-in-memory-store-ephemeral-use","level":3,"title":"Use a faster in-memory store (ephemeral use)","text":"<pre><code>export KERNELBOX_STORE_BACKEND=memory\n</code></pre> <p>The kernel registry lives only in the current process. Great for short-lived scripts or tests.</p>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#move-data-files-to-a-custom-location","level":3,"title":"Move data files to a custom location","text":"<pre><code>export KERNELBOX_HOME=/var/kernelbox\n# Registry → /var/kernelbox/registry.json\n# Runtime  → /var/kernelbox/runtime/\n</code></pre>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#increase-timeout-for-heavy-computations","level":3,"title":"Increase timeout for heavy computations","text":"<pre><code>export KERNELBOX_EXECUTION_TIMEOUT=600   # 10 minutes\n</code></pre>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#increase-output-capture-limit","level":3,"title":"Increase output capture limit","text":"<pre><code>export KERNELBOX_OUTPUT_CHAR_LIMIT=50000\n</code></pre>","path":["Getting Started","Configuration"],"tags":[]},{"location":"configuration/#reading-config-in-python","level":2,"title":"Reading config in Python","text":"<pre><code>from kernelbox import KernelBoxConfig\n\nconfig = KernelBoxConfig.from_env()\nprint(config.execution_timeout)   # 60.0\nprint(config.store_backend)       # \"file\"\nprint(config.runtime_dir)         # PosixPath('/tmp/kernelbox/runtime')\n</code></pre>","path":["Getting Started","Configuration"],"tags":[]},{"location":"dev_guide/","level":1,"title":"Development Guide","text":"<p>Welcome to the KernelBox development guide! This document outlines the local setup, project structure, and workflow for contributing to the codebase.</p>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#local-setup","level":2,"title":"Local Setup","text":"","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#prerequisites","level":3,"title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li><code>uv</code> (fast Python package installer and resolver)</li> <li>Git</li> </ul>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#installation","level":3,"title":"Installation","text":"<ol> <li> <p>Clone the repository:</p> <pre><code>git clone &lt;your-repo-url&gt;\ncd KernelBox\n</code></pre> </li> <li> <p>Sync dependencies using <code>uv</code>:</p> <pre><code>uv sync --extra dev\n</code></pre> <p>This will create a <code>.venv</code> directory and install all dependencies (including dev dependencies) listed in the <code>uv.lock</code> file.</p> </li> <li> <p>Activate the virtual environment:</p> <ul> <li>Windows: <code>.venv\\Scripts\\activate</code></li> <li>Linux/macOS: <code>source .venv/bin/activate</code></li> </ul> </li> </ol>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#project-architecture","level":2,"title":"Project Architecture","text":"<p>KernelBox is divided into several key components located in <code>src/kernelbox/</code>:</p> Module Purpose <code>api.py</code> Public programmatic Python API <code>cli/</code> Command-line interface using <code>argparse</code> <code>config/</code> Configuration loading and defaults <code>core/manager.py</code> Kernel lifecycle — create, list, ping, restart, destroy <code>core/executor.py</code> Code execution and output capture <code>core/retry.py</code> Retry controller (<code>execute_with_retry</code>) <code>server/app.py</code> FastAPI route handlers <code>server/models.py</code> Request / response schemas <code>store/registry.py</code> Kernel registry (file or memory backend)","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#development-workflow","level":2,"title":"Development Workflow","text":"","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#running-the-api-server-locally","level":3,"title":"Running the API Server Locally","text":"<p>Use the official FastAPI CLI. Run on port <code>8080</code> to avoid conflicts with the Zensical docs server (port <code>8000</code>):</p> <pre><code>uv run fastapi dev --port 8080\n</code></pre> <p>The server will be available at <code>http://127.0.0.1:8080</code>. Access Swagger UI at <code>http://127.0.0.1:8080/docs</code>.</p>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#running-the-cli","level":3,"title":"Running the CLI","text":"<pre><code>uv run kernelbox --help\n</code></pre>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#running-tests","level":3,"title":"Running Tests","text":"<p>We use <code>pytest</code> for testing. To run the test suite:</p> <pre><code>uv run pytest\n</code></pre> <p>Ensure all tests pass before submitting a pull request.</p>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#running-with-docker","level":3,"title":"Running with Docker","text":"<p>To test KernelBox in its isolated, production-ready Docker environment:</p> <pre><code># Build the image and run it in the background\ndocker-compose up --build -d\n\n# Check the health of the running container\ncurl http://localhost:8080/health\n\n# Stop and remove the container\ndocker-compose down\n</code></pre> <p>Docker is the recommended way to securely execute code, as it drops Linux capabilities and runs as a restricted <code>sandbox_user</code>.</p>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#building-documentation","level":3,"title":"Building Documentation","text":"<p>The documentation is built using Zensical with the Material theme.</p> <p>Serve the docs locally:</p> <pre><code>uv run zensical serve\n</code></pre> <p>Then open <code>http://127.0.0.1:8000</code> in your browser.</p> <p>Port separation</p> <p>The docs server runs on port <code>8000</code> and the FastAPI server on port <code>8080</code> to avoid conflicts.</p> <p>Build the static HTML:</p> <pre><code>uv run zensical build\n</code></pre>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#code-quality-and-linting","level":3,"title":"Code Quality and Linting","text":"<p>Please ensure your code follows the project's styling guidelines. We recommend using <code>ruff</code> for formatting and linting:</p> <pre><code>uv run ruff check .\nuv run ruff format .\n</code></pre>","path":["Advanced","Development Guide"],"tags":[]},{"location":"dev_guide/#contributing","level":2,"title":"Contributing","text":"<ol> <li> <p>Fork the repository and create a new branch for your feature or bug fix.</p> </li> <li> <p>Write clear, concise commit messages.</p> </li> <li> <p>Include tests for any new features or critical bug fixes.</p> </li> <li> <p>Update the documentation if your changes affect the user-facing API or CLI.</p> </li> <li> <p>Open a Pull Request!</p> </li> </ol>","path":["Advanced","Development Guide"],"tags":[]},{"location":"examples/","level":1,"title":"Examples","text":"<p>Real-world snippets showing KernelBox in action.</p>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#basic-stateful-session","level":2,"title":"Basic stateful session","text":"<pre><code>from kernelbox import get_or_create, execute, destroy\n\nkernel = get_or_create(\"demo\")\n\n# Variables persist between calls\nexecute(kernel, \"numbers = [1, 2, 3, 4, 5]\")\nresult = execute(kernel, \"sum(numbers)\")\n\nprint(result.return_value)   # 15\nprint(result.status)         # success\nprint(result.duration_ms)    # e.g. 8\n\ndestroy(\"demo\")\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#data-science-pipeline","level":2,"title":"Data science pipeline","text":"<pre><code>kernel = get_or_create(\"data-work\")\n\nexecute(kernel, \"import pandas as pd\")\nexecute(kernel, \"df = pd.DataFrame({'a': [1, 2, 3], 'b': [4, 5, 6]})\")\nexecute(kernel, \"df['c'] = df['a'] + df['b']\")\n\nresult = execute(kernel, \"df\")\nprint(result.output)       # printed repr of the dataframe\nprint(result.return_value) # string repr of last expression\n\ndestroy(\"data-work\")\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#capture-printed-output-vs-return-value","level":2,"title":"Capture printed output vs. return value","text":"<pre><code># output captures print() calls\nresult = execute(kernel, \"print('hello world')\")\nprint(result.output)        # hello world\\n\nprint(result.return_value)  # None\n\n# return_value captures the last expression\nresult = execute(kernel, \"2 ** 8\")\nprint(result.output)        # (empty)\nprint(result.return_value)  # 256\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#handle-errors-gracefully","level":2,"title":"Handle errors gracefully","text":"<pre><code>result = execute(kernel, \"1 / 0\")\n\nif not result.ok:\n    print(result.status)           # error\n    print(result.error.ename)      # ZeroDivisionError\n    print(result.error.evalue)     # division by zero\n    print(result.error.traceback)  # list of traceback lines\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#self-correcting-agent-with-retry","level":2,"title":"Self-correcting agent with retry","text":"<pre><code>from kernelbox import execute_with_retry, get_or_create\n\nkernel = get_or_create(\"agent\")\n\ndef fix_code(result, attempt):\n    \"\"\"Called whenever execution fails. Return new code or None to retry the same.\"\"\"\n    print(f\"Attempt {attempt} failed: {result.error.ename}: {result.error.evalue}\")\n    # In a real agent you'd ask the LLM to fix the code.\n    # Here we hardcode the fix:\n    return \"x = 41\\nx + 1\"\n\nresult = execute_with_retry(kernel, \"x + 1\", on_error_fn=fix_code)\n\nprint(result.status)         # success\nprint(result.return_value)   # 42\nprint(len(result.attempts))  # 2\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#bash-commands","level":2,"title":"Bash commands","text":"<pre><code>result = execute(kernel, \"echo $HOME &amp;&amp; ls -la\", language=\"bash\")\nprint(result.output)\n</code></pre> <p>Bash execution note</p> <p>Bash runs through IPython's <code>%%bash</code> magic. It is not a fully interactive shell — things like <code>cd</code> will not persist to subsequent calls.</p>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#check-if-output-was-truncated","level":2,"title":"Check if output was truncated","text":"<pre><code>result = execute(kernel, \"print('x' * 20000)\")\n\nif result.truncated:\n    print(\"Output was cut — increase KERNELBOX_OUTPUT_CHAR_LIMIT to see all of it.\")\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#http-api-from-python","level":2,"title":"HTTP API from Python","text":"<pre><code>import httpx\n\n# Start the server first: uv run fastapi dev --port 8080\n\nresponse = httpx.post(\n    \"http://127.0.0.1:8080/sessions/demo/execute\",\n    json={\"code\": \"21 * 2\"},\n)\n\nresult = response.json()\nprint(result[\"return_value\"])  # 42\nprint(result[\"status\"])        # success\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"examples/#pre-warm-a-kernel-with-startup-code","level":2,"title":"Pre-warm a kernel with startup code","text":"<p>Use <code>KERNELBOX_STARTUP_CODE</code> so every kernel starts with your imports ready:</p> <pre><code>export KERNELBOX_STARTUP_CODE=\"import numpy as np; import pandas as pd\"\n</code></pre> <pre><code>kernel = get_or_create(\"warm\")\nresult = execute(kernel, \"np.array([1, 2, 3]).mean()\")\nprint(result.return_value)  # 2.0  (numpy was already imported)\n</code></pre>","path":["Usage & Tutorials","Examples"],"tags":[]},{"location":"requirements/","level":1,"title":"Prerequisites","text":"<p>You need these tools before setting up KernelBox.</p>","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"requirements/#required","level":2,"title":"Required","text":"Tool Why Python 3.10 or newer Runs KernelBox <code>uv</code> Creates the virtual environment and installs packages <code>jupyter_client</code> Talks to the IPython kernel <code>ipykernel</code> Starts Python kernels","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"requirements/#for-the-http-api","level":2,"title":"For the HTTP API","text":"Tool Why FastAPI Provides the REST API <code>fastapi-cli</code> Runs the server with <code>fastapi dev</code> and <code>fastapi run</code> Uvicorn Serves the ASGI app","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"requirements/#for-docker-recommended-for-isolation","level":2,"title":"For Docker (Recommended for Isolation)","text":"Tool Why Docker Creates an isolated container Docker Compose Simplifies building and running the container","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"requirements/#for-documentation","level":2,"title":"For Documentation","text":"Tool Why Zensical Builds and serves the documentation site mike Publishes versioned docs to GitHub Pages","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"requirements/#supported-operating-systems","level":2,"title":"Supported Operating Systems","text":"<p>KernelBox works on:</p> <ul> <li>Windows</li> <li>macOS</li> <li>Linux</li> </ul> <p>The bootstrap script detects the OS and chooses the right <code>uv</code> installer automatically.</p>","path":["Getting Started","Prerequisites"],"tags":[]},{"location":"run/","level":1,"title":"Run KernelBox","text":"<p>You can run KernelBox in three ways.</p>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"run/#run-via-docker-recommended","level":2,"title":"Run via Docker (Recommended)","text":"<p>The most secure way to run the FastAPI service is via Docker. This drops all host privileges and prevents executed agent code from reading or destroying local files.</p> <pre><code># Start the container in the background\ndocker-compose up -d\n</code></pre> <p>The API will be available at <code>http://localhost:8080</code>.</p> <p>To stop the service, run: <pre><code>docker-compose down\n</code></pre></p>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"run/#run-the-fastapi-service","level":2,"title":"Run the FastAPI service","text":"<p>Use the official FastAPI CLI. We use port <code>8080</code> to prevent conflicts with the Zensical docs server.</p> <p>Development mode:</p> <pre><code>uv run fastapi dev --port 8080\n</code></pre> <p>Production-style mode:</p> <pre><code>uv run fastapi run --port 8080\n</code></pre> <p>Open the API docs:</p> <pre><code>http://127.0.0.1:8080/docs\n</code></pre> <p>Other docs:</p> <pre><code>http://127.0.0.1:8080/redoc\nhttp://127.0.0.1:8080/openapi.json\n</code></pre>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"run/#run-python-code","level":2,"title":"Run Python code","text":"<pre><code>from kernelbox import get_or_create, execute\n\nkernel = get_or_create(\"demo\")\nresult = execute(kernel, \"1 + 1\")\n\nprint(result.return_value)\n</code></pre>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"run/#run-cli-commands","level":2,"title":"Run CLI commands","text":"<pre><code>uv run kernelbox create --name demo\nuv run kernelbox exec --name demo \"1 + 1\"\nuv run kernelbox destroy --name demo\n</code></pre>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"run/#stop-everything","level":2,"title":"Stop everything","text":"<p>Destroy all registered kernels:</p> <pre><code>uv run kernelbox wipe\n</code></pre>","path":["Getting Started","Run KernelBox"],"tags":[]},{"location":"security/","level":1,"title":"Security Overview","text":"<p>Before deploying KernelBox in a production or public-facing environment, it is crucial to understand its security model and current limitations. KernelBox is designed as a lightweight, developer-focused tool, and by default, it does not include robust security features for untrusted environments.</p> <p>Read this before production</p> <p>KernelBox runs code with the same OS privileges as the user who started the server. The FastAPI server has no authentication by default. Always read this page and apply the mitigations before exposing the service to any network.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#mitigating-the-risks-with-docker","level":2,"title":"Mitigating the risks with Docker","text":"<p>KernelBox ships with a production-ready <code>Dockerfile</code> and <code>docker-compose.yml</code> that address the most critical risks out of the box.</p> <p>What the included Docker setup does:</p> <ul> <li>Runs the service as a non-root user (<code>sandbox_user</code>) — executed code cannot modify system files.</li> <li>Mounts the filesystem as read-only — the container cannot write to the host.</li> <li>Uses tmpfs for the <code>/tmp</code> scratch space — writes are in-memory only and discarded on container stop.</li> <li>Drops all Linux capabilities — the process cannot acquire elevated privileges even if exploited.</li> </ul> <p>Start the hardened container:</p> <pre><code>docker-compose up --build -d\n\n# Verify it is running\ncurl http://localhost:8080/health\n\n# Stop\ndocker-compose down\n</code></pre> <p>Recommended for production</p> <p>Running KernelBox inside Docker is the single most effective way to contain the RCE risk. If you cannot use Docker, bind the API to <code>127.0.0.1</code> and restrict access at the network level.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#known-security-flaws-considerations","level":2,"title":"Known Security Flaws &amp; Considerations","text":"","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#1-unauthenticated-api-endpoints","level":3,"title":"1. Unauthenticated API Endpoints","text":"<p>Flaw: The FastAPI server exposes endpoints (e.g., <code>/sessions/{name}/execute</code>) without any authentication — no API keys, OAuth, or JWTs.</p> <p>Risk: Anyone who can reach the API port can execute arbitrary Python or Bash code on the server.</p> <p>Mitigation: Do not expose the KernelBox API directly to the public internet. Run it inside Docker, or put it behind a reverse proxy (Nginx, Caddy, Traefik) that enforces authentication. In development, bind only to <code>localhost</code> (<code>127.0.0.1</code>).</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#2-arbitrary-code-execution-no-sandboxing","level":3,"title":"2. Arbitrary Code Execution (No Sandboxing)","text":"<p>Flaw: KernelBox uses <code>jupyter_client</code> to spawn IPython kernels as child processes. These kernels inherit the full OS user context — filesystem access, environment variables, and network access of the parent process.</p> <p>Risk: Code executed via KernelBox has direct Remote Code Execution (RCE) capabilities. Without containerisation, there is nothing preventing executed code from reading secrets, modifying files, or making network calls.</p> <p>Mitigation: Use the included Docker setup (see above). If running natively, only execute trusted code. For untrusted agent-generated code, always run inside the Docker container.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#3-windows-acl-workaround","level":3,"title":"3. Windows ACL Workaround","text":"<p>Flaw: To avoid <code>PermissionError</code> on Windows, KernelBox patches Jupyter's <code>win32_restrict_file_to_user</code> ACL restriction when writing kernel connection files.</p> <p>Risk: Kernel connection files contain the HMAC keys used by the Jupyter wire protocol. If created with overly permissive ACLs, other users on the same Windows machine may be able to read them and connect to the kernel directly.</p> <p>Mitigation: Use KernelBox on a single-user Windows machine, or prefer a Linux/Docker environment where standard Unix file permissions apply correctly.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#4-missing-cors-configuration","level":3,"title":"4. Missing CORS Configuration","text":"<p>Flaw: The FastAPI application does not configure Cross-Origin Resource Sharing (CORS).</p> <p>Risk: If you build a browser-based frontend for KernelBox, all requests will be blocked by the browser's CORS policy. If CORS is added permissively in the future, it could allow unauthorised web origins to call the API.</p> <p>Mitigation: Add <code>CORSMiddleware</code> in <code>src/kernelbox/server/app.py</code> with a strict <code>allow_origins</code> list before enabling any browser access.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#5-kernel-connection-file-leftovers","level":3,"title":"5. Kernel Connection File Leftovers","text":"<p>Flaw: If the KernelBox service crashes unexpectedly, Jupyter kernel connection files (containing HMAC keys) may be left behind in <code>KERNELBOX_RUNTIME_DIR</code>.</p> <p>Risk: Orphaned connection files expose kernel credentials and accumulate on disk indefinitely.</p> <p>Mitigation: Periodically clean up <code>KERNELBOX_RUNTIME_DIR</code>. When using Docker, tmpfs handles this automatically — files are wiped when the container stops.</p>","path":["Advanced","Security Overview"],"tags":[]},{"location":"security/#best-practices-checklist","level":2,"title":"Best Practices Checklist","text":"<p>Before pushing to a repository or deploying</p> <ul> <li>Do not commit secrets — add <code>.env</code> files, connection files, and any credential files to <code>.gitignore</code>.</li> <li>Document the risks — make sure everyone using the deployment knows about RCE and the lack of authentication.</li> <li>Run a security linter — use <code>bandit</code> or <code>safety</code> in CI to catch newly introduced Python vulnerabilities.</li> <li>Use Docker in production — the included setup is the recommended baseline for any non-local deployment.</li> <li>Bind to localhost in development — <code>uv run fastapi dev --port 8080</code> binds to <code>127.0.0.1</code> by default; do not change this without a reverse proxy in front.</li> </ul>","path":["Advanced","Security Overview"],"tags":[]},{"location":"setup/","level":1,"title":"Setup","text":"<p>KernelBox uses <code>uv</code>.</p> <p>Do not use <code>pip</code> for this project.</p>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#1-clone-or-open-the-project","level":2,"title":"1. Clone or open the project","text":"<p>Open a terminal in the project folder.</p> <pre><code>cd F:\\Projects\\KernelBox\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#2-docker-setup-recommended","level":2,"title":"2. Docker Setup (Recommended)","text":"<p>To run KernelBox securely inside an isolated container, bypassing the need for local Python or <code>uv</code> installation, make sure Docker is installed and running, then simply build the image:</p> <pre><code>docker-compose build\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#3-bootstrap-the-project","level":2,"title":"3. Bootstrap the project","text":"<p>Run:</p> <pre><code>python scripts/bootstrap.py\n</code></pre> <p>The script will:</p> <ul> <li>detect your operating system</li> <li>install <code>uv</code> if it is missing</li> <li>create <code>.venv</code></li> <li>install project dependencies</li> <li>install development tools</li> </ul>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#4-manual-setup","level":2,"title":"4. Manual setup","text":"<p>If <code>uv</code> is already installed, you can run the commands yourself.</p> <pre><code>uv venv\nuv sync --extra dev\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#5-check-the-install","level":2,"title":"5. Check the install","text":"<p>Run the tests:</p> <pre><code>uv run pytest\n</code></pre> <p>Expected result:</p> <pre><code>6 passed\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#6-build-the-docs","level":2,"title":"6. Build the docs","text":"<pre><code>uv run zensical build\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"setup/#7-serve-the-docs-locally","level":2,"title":"7. Serve the docs locally","text":"<pre><code>uv run zensical serve\n</code></pre> <p>Open:</p> <pre><code>http://127.0.0.1:8000\n</code></pre>","path":["Getting Started","Setup"],"tags":[]},{"location":"version/","level":1,"title":"Version","text":"<p>Current package version:</p> <pre><code>0.1.0\n</code></pre> <p>The version is defined in <code>pyproject.toml</code>.</p> <pre><code>[project]\nname = \"kernelbox\"\nversion = \"0.1.0\"\n</code></pre>","path":["Reference","Version"],"tags":[]},{"location":"version/#versioned-docs","level":2,"title":"Versioned docs","text":"<p>The docs are ready for versioned publishing with <code>mike</code>.</p> <p>Build local docs:</p> <pre><code>uv run zensical build\n</code></pre> <p>Serve local docs:</p> <pre><code>uv run zensical serve\n</code></pre> <p>Publish a version with mike:</p> <pre><code>uv run mike deploy --push --update-aliases 0.1.0 latest\n</code></pre> <p>Set the default docs version:</p> <pre><code>uv run mike set-default --push latest\n</code></pre>","path":["Reference","Version"],"tags":[]},{"location":"tutorials/cli/","level":1,"title":"CLI","text":"<p>The <code>kernelbox</code> CLI manages kernels and executes code from the terminal. Every command outputs JSON — easy to pipe into <code>jq</code> or parse in scripts.</p>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#commands-at-a-glance","level":2,"title":"Commands at a glance","text":"Command What it does <code>create --name &lt;name&gt;</code> Start a new named kernel <code>list</code> List all registered kernels <code>list --refresh</code> List kernels and re-ping each for live status <code>status --name &lt;name&gt;</code> Check if a kernel is alive <code>exec --name &lt;name&gt; &lt;code&gt;</code> Run Python code <code>exec --name &lt;name&gt; --bash &lt;code&gt;</code> Run a bash command <code>exec ... --timeout &lt;secs&gt;</code> Override the execution timeout <code>restart --name &lt;name&gt;</code> Restart a kernel (clears all variables) <code>destroy --name &lt;name&gt;</code> Stop a kernel and remove it from the registry <code>wipe</code> Destroy all registered kernels <p>You can use <code>--id &lt;kernel-id&gt;</code> instead of <code>--name</code> in any command that accepts an identifier.</p>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#walkthrough","level":2,"title":"Walkthrough","text":"","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#create-a-kernel","level":3,"title":"Create a kernel","text":"<pre><code>uv run kernelbox create --name demo\n</code></pre> <pre><code>{\n  \"connection_file\": \".kernelbox/runtime/kernel-abc123.json\",\n  \"created_at\": \"2026-05-14T06:00:00+00:00\",\n  \"kernel_id\": \"abc123\",\n  \"name\": \"demo\",\n  \"status\": \"running\"\n}\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#run-python-code","level":3,"title":"Run Python code","text":"<pre><code>uv run kernelbox exec --name demo \"2 ** 10\"\n</code></pre> <pre><code>{\n  \"duration_ms\": 8,\n  \"output\": \"\",\n  \"return_value\": \"1024\",\n  \"status\": \"success\"\n}\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#state-persists-between-calls","level":3,"title":"State persists between calls","text":"<pre><code>uv run kernelbox exec --name demo \"x = 42\"\nuv run kernelbox exec --name demo \"x * 2\"\n# return_value: \"84\"\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#run-a-bash-command","level":3,"title":"Run a bash command","text":"<pre><code>uv run kernelbox exec --name demo --bash \"echo hello &amp;&amp; pwd\"\n</code></pre> <pre><code>{\n  \"output\": \"hello\\n/your/working/dir\\n\",\n  \"status\": \"success\"\n}\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#set-a-custom-timeout","level":3,"title":"Set a custom timeout","text":"<pre><code>uv run kernelbox exec --name demo --timeout 5 \"import time; time.sleep(10)\"\n# status: \"timeout\"\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#check-kernel-health","level":3,"title":"Check kernel health","text":"<pre><code>uv run kernelbox status --name demo\n</code></pre> <pre><code>{\n  \"alive\": true,\n  \"kernel\": { \"name\": \"demo\", \"status\": \"running\", ... }\n}\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#list-all-kernels","level":3,"title":"List all kernels","text":"<pre><code>uv run kernelbox list\nuv run kernelbox list --refresh   # re-pings each kernel for a live status check\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#restart-a-kernel","level":3,"title":"Restart a kernel","text":"<p>Clears all variables — equivalent to a fresh kernel.</p> <pre><code>uv run kernelbox restart --name demo\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#destroy-a-single-kernel","level":3,"title":"Destroy a single kernel","text":"<pre><code>uv run kernelbox destroy --name demo\n# { \"destroyed\": true }\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#wipe-everything","level":3,"title":"Wipe everything","text":"<pre><code>uv run kernelbox wipe\n# { \"destroyed\": [\"abc123\", \"def456\"] }\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#tips-for-agentic-workflows","level":2,"title":"Tips for agentic workflows","text":"<p>The CLI is convenient for one-off commands. For agents that generate large, multi-line code blocks, prefer the Python API or HTTP API — passing multi-line code through shell arguments involves quoting and escaping that can silently corrupt the code before it even reaches the kernel.</p> <pre><code># This is fragile for multi-line code:\nuv run kernelbox exec --name demo \"x = 1\\ny = x + 1\\nprint(y)\"\n\n# Better: use the Python API or HTTP endpoint for generated code\n</code></pre>","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/cli/#fastapi-server-cli","level":2,"title":"FastAPI server CLI","text":"<p>To start the HTTP service:</p> <pre><code>uv run fastapi dev --port 8080    # development, hot-reload\nuv run fastapi run --port 8080    # production\n</code></pre> URL Description <code>http://127.0.0.1:8080/docs</code> Swagger UI <code>http://127.0.0.1:8080/redoc</code> ReDoc <code>http://127.0.0.1:8080/openapi.json</code> OpenAPI schema","path":["Usage & Tutorials","CLI"],"tags":[]},{"location":"tutorials/http-api/","level":1,"title":"HTTP API","text":"<p>KernelBox includes a FastAPI service that exposes the full kernel management API over HTTP. Use it when your agent or tool is not written in Python, or when you want to call KernelBox from a separate process.</p>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#start-the-server","level":2,"title":"Start the server","text":"<pre><code>uv run fastapi dev --port 8080    # development, hot-reload\nuv run fastapi run --port 8080    # production\n</code></pre> <p>Port recommendation</p> <p>Use port <code>8080</code> to avoid conflicts with the Zensical docs server which runs on port <code>8000</code>.</p>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#interactive-docs","level":2,"title":"Interactive docs","text":"<p>Once running, open:</p> <ul> <li>Swagger UI — <code>http://127.0.0.1:8080/docs</code> — try every endpoint in the browser</li> <li>ReDoc — <code>http://127.0.0.1:8080/redoc</code></li> <li>OpenAPI schema — <code>http://127.0.0.1:8080/openapi.json</code></li> </ul>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#endpoints","level":2,"title":"Endpoints","text":"","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#execute-code-in-a-named-session","level":3,"title":"Execute code in a named session","text":"<pre><code>POST /sessions/{name}/execute\n</code></pre> <p>Creates the session if it doesn't exist, then runs the code. Sessions are persistent — state carries over between calls with the same <code>name</code>.</p> <p>Request:</p> <pre><code>{\n  \"code\": \"x = 42\\nx * 2\",\n  \"language\": \"python\",\n  \"timeout\": null\n}\n</code></pre> <p>Response:</p> <pre><code>{\n  \"status\": \"success\",\n  \"output\": \"\",\n  \"stderr\": \"\",\n  \"return_value\": \"84\",\n  \"execution_count\": 1,\n  \"duration_ms\": 11,\n  \"truncated\": false,\n  \"error\": null\n}\n</code></pre> <p>curl example:</p> <pre><code>curl -X POST http://127.0.0.1:8080/sessions/demo/execute \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"code\": \"2 + 2\"}'\n</code></pre> <p>Python (<code>httpx</code>) example:</p> <pre><code>import httpx\n\nresponse = httpx.post(\n    \"http://127.0.0.1:8080/sessions/demo/execute\",\n    json={\"code\": \"sum([1, 2, 3, 4, 5])\"},\n)\nresult = response.json()\nprint(result[\"return_value\"])  # 15\nprint(result[\"status\"])        # success\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#execute-with-retry","level":3,"title":"Execute with retry","text":"<pre><code>POST /sessions/{name}/execute_with_retry\n</code></pre> <p>Runs code with automatic retries. Include <code>on_error_code</code> — the code to run after each failure.</p> <p>Request:</p> <pre><code>{\n  \"code\": \"x + 1\",\n  \"on_error_code\": \"x = 41\\nx + 1\",\n  \"max_attempts\": 3\n}\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#list-all-kernels","level":3,"title":"List all kernels","text":"<pre><code>GET /kernels\n</code></pre> <p>Returns all registered kernels.</p> <pre><code>curl http://127.0.0.1:8080/kernels\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#get-a-single-kernel","level":3,"title":"Get a single kernel","text":"<pre><code>GET /kernels/{kernel_id}\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#create-a-kernel","level":3,"title":"Create a kernel","text":"<pre><code>POST /kernels\n</code></pre> <p>Request:</p> <pre><code>{ \"name\": \"my-kernel\" }\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#restart-a-kernel","level":3,"title":"Restart a kernel","text":"<pre><code>POST /kernels/{kernel_id}/restart\n</code></pre> <p>Clears all variables — equivalent to a fresh kernel.</p>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#destroy-a-kernel","level":3,"title":"Destroy a kernel","text":"<pre><code>DELETE /kernels/{kernel_id}\n</code></pre>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#health-check","level":3,"title":"Health check","text":"<pre><code>GET /health\n</code></pre> <p>Returns <code>{ \"status\": \"ok\" }</code>. Useful for Docker health checks and load balancers.</p>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/http-api/#running-with-docker","level":2,"title":"Running with Docker","text":"<p>The <code>docker-compose.yml</code> exposes port <code>8080</code> on your host:</p> <pre><code>docker-compose up --build -d\ncurl http://localhost:8080/health\n</code></pre> <p>No authentication</p> <p>The HTTP API has no authentication. In production, always bind to <code>127.0.0.1</code> or place the server behind a reverse proxy (Nginx, Caddy) that enforces authentication.</p>","path":["Usage & Tutorials","HTTP API"],"tags":[]},{"location":"tutorials/python-api/","level":1,"title":"Python API","text":"<p>The Python API is the fastest way to use KernelBox. Import four functions and you have everything you need.</p> <pre><code>from kernelbox import get_or_create, execute, execute_with_retry, destroy\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#get_or_createname-start-or-reuse-a-session","level":2,"title":"<code>get_or_create(name)</code> — start or reuse a session","text":"<p>Returns a live <code>KernelRecord</code>. If a kernel with this name is already registered and running, you get it back. If not, a new kernel process is started.</p> <pre><code>kernel = get_or_create(\"my-session\")\n</code></pre> <p>You can also pass a kernel ID string directly to <code>execute()</code> — KernelBox will look it up for you.</p>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#executekernel-code-run-code-get-a-result","level":2,"title":"<code>execute(kernel, code)</code> — run code, get a result","text":"<pre><code>result = execute(kernel, \"x = 10\\nx * 2\")\n</code></pre> <p><code>execute</code> accepts optional keyword arguments:</p> Argument Type Default Description <code>language</code> <code>str</code> <code>\"python\"</code> <code>\"python\"</code> or <code>\"bash\"</code> <code>timeout</code> <code>float \\| None</code> config value Override the default execution timeout in seconds","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#the-executionresult-object","level":3,"title":"The <code>ExecutionResult</code> object","text":"<p>Every call to <code>execute</code> returns an <code>ExecutionResult</code> dataclass:</p> Field Type Description <code>status</code> <code>str</code> <code>\"success\"</code> · <code>\"error\"</code> · <code>\"timeout\"</code> · <code>\"max_attempts_exceeded\"</code> <code>ok</code> <code>bool</code> <code>True</code> when <code>status == \"success\"</code> — convenient shortcut <code>output</code> <code>str</code> Captured stdout <code>stderr</code> <code>str</code> Captured stderr <code>return_value</code> <code>str \\| None</code> String representation of the last expression's value <code>error</code> <code>ErrorInfo \\| None</code> Structured error with <code>.ename</code>, <code>.evalue</code>, <code>.traceback</code> <code>execution_count</code> <code>int \\| None</code> IPython execution counter <code>duration_ms</code> <code>int</code> Wall-clock execution time in milliseconds <code>outputs</code> <code>list[OutputItem]</code> All raw kernel output items (stream, display_data, etc.) <code>truncated</code> <code>bool</code> <code>True</code> if output was cut at <code>KERNELBOX_OUTPUT_CHAR_LIMIT</code> <code>attempts</code> <code>list[AttemptSnapshot]</code> Per-attempt history (populated by <code>execute_with_retry</code>)","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#example-reading-all-fields","level":3,"title":"Example — reading all fields","text":"<pre><code>result = execute(kernel, \"print('hello')\\n2 + 2\")\n\nprint(result.status)          # success\nprint(result.ok)              # True\nprint(result.output)          # hello\nprint(result.return_value)    # 4\nprint(result.duration_ms)     # e.g. 12\nprint(result.truncated)       # False\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#example-handling-an-error","level":3,"title":"Example — handling an error","text":"<pre><code>result = execute(kernel, \"1 / 0\")\n\nif not result.ok:\n    print(result.error.ename)   # ZeroDivisionError\n    print(result.error.evalue)  # division by zero\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#example-bash-execution","level":3,"title":"Example — bash execution","text":"<pre><code>result = execute(kernel, \"echo $PWD &amp;&amp; ls\", language=\"bash\")\nprint(result.output)\n</code></pre> <p>Bash execution note</p> <p>Bash runs through IPython's <code>%%bash</code> cell magic, not a real shell subprocess. Environment variables and the working directory may differ from your terminal session.</p>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#execute_with_retry-self-correcting-execution","level":2,"title":"<code>execute_with_retry</code> — self-correcting execution","text":"<p>Designed for AI agents that can generate a fix when code fails.</p> <pre><code>result = execute_with_retry(\n    kernel,\n    code,\n    on_error_fn,\n    *,\n    max_attempts=None,   # defaults to KERNELBOX_MAX_RETRIES (5)\n    language=\"python\",\n    timeout=None,\n)\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#how-it-works","level":3,"title":"How it works","text":"<ol> <li>KernelBox executes <code>code</code>.</li> <li>If it succeeds → return <code>ExecutionResult</code> immediately.</li> <li>If it fails → call <code>on_error_fn(result, attempt)</code>.</li> <li>Return a new code string → use that on the next attempt.</li> <li>Return <code>None</code> → retry the same code.</li> <li>After <code>max_attempts</code> failures → return with <code>status=\"max_attempts_exceeded\"</code>.</li> </ol> <pre><code>from kernelbox import execute_with_retry, get_or_create\n\nkernel = get_or_create(\"agent\")\n\ndef fix(result, attempt):\n    print(f\"Attempt {attempt} failed: {result.error.evalue}\")\n    # Ask your LLM for a fix, or hardcode one:\n    return \"x = 41\\nx + 1\"\n\nresult = execute_with_retry(kernel, \"x + 1\", on_error_fn=fix)\n\nprint(result.status)        # success\nprint(result.return_value)  # 42\nprint(len(result.attempts)) # 2 — the failed attempt and the fixed one\n</code></pre> <p>Each entry in <code>result.attempts</code> is an <code>AttemptSnapshot</code> with <code>attempt</code>, <code>code</code>, <code>status</code>, <code>output</code>, <code>stderr</code>, and <code>error</code>.</p>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#destroyname-clean-up","level":2,"title":"<code>destroy(name)</code> — clean up","text":"<p>Shuts down the kernel subprocess and removes it from the registry. Returns <code>True</code> if a kernel was found and destroyed.</p> <pre><code>destroy(\"my-session\")\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]},{"location":"tutorials/python-api/#stateful-sessions-in-practice","level":2,"title":"Stateful sessions in practice","text":"<p>Because state persists, you can build up complex data pipelines step by step:</p> <pre><code>kernel = get_or_create(\"pipeline\")\n\nexecute(kernel, \"import pandas as pd\")\nexecute(kernel, \"df = pd.read_csv('data.csv')\")\nexecute(kernel, \"df = df.dropna()\")\n\nresult = execute(kernel, \"df.shape\")\nprint(result.return_value)  # (1823, 12)\n\nexecute(kernel, \"df.to_parquet('clean.parquet')\")\ndestroy(\"pipeline\")\n</code></pre>","path":["Usage & Tutorials","Python API"],"tags":[]}]}